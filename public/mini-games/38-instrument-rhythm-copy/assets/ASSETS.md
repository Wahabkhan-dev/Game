# Game 38 — Instrument Rhythm Copy — Assets

The xylophone pads are colored CSS buttons and the notes play as
Web Audio tones (offline) — so the game works with NO files.

Optional real note clips (short, ~0.3s each). If present they play
instead of the synth tone; index matches pad order (red, yellow,
green, blue):
| File | Pad |
|------|-----|
| assets/note-0.mp3 | Red (low C) |
| assets/note-1.mp3 | Yellow (E) |
| assets/note-2.mp3 | Green (G) |
| assets/note-3.mp3 | Blue (high C) |

Optional image: assets/xylophone.png or drum-pad art if you want to
replace the CSS pads (would require small tweaks in index.html/game.js).
