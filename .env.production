# Production build env — used by `vite build` (mode=production).
VITE_API_URL=https://api.demolinkwebiste.com
# TEST-ONLY: WordPress isn't set up yet, so this points at the backend's own
# test-login stub (ENABLE_TEST_LOGIN=true) instead of glendagame.demolinkwebiste.com.
# Swap back once the real WordPress + Game Handoff plugin login is ready.
VITE_WP_URL=https://api.demolinkwebiste.com
